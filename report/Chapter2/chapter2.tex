\setcounter{chapter}{1}
\chapter{Analysis and Requirements Specification}
\label{chap:analysis}
\graphicspath{{Chapter2/figures/}}

%==============================================================================
\pagestyle{fancy}
\fancyhf{}
\fancyhead[R]{\bfseries\rightmark}
\fancyfoot[R]{\thepage}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\footrulewidth}{0pt}
\renewcommand{\chaptermark}[1]{\markboth{{\chaptername~\thechapter.\ #1}}{}}
\renewcommand{\sectionmark}[1]{\markright{\thechapter.\thesection~ #1}}

\begin{spacing}{1.2}
%==============================================================================

\section*{Introduction}
This chapter specifies what CLMPilot is required to do. We proceed from
the users inward: we first catalogue the actors who interact with the
platform and expand the use cases they initiate -- from a single
high-level overview down to the individual cases of each functional
area, with the most consequential scenarios described step by step. From
those use cases we then derive the functional requirements, stated at
the level of capability rather than implementation, and we close with
the non-functional requirements: the qualities the platform must exhibit
while delivering them.

%--------------------------------------------------------------------
\section{Actors and use cases}
\label{sec:actors}

The actor model is intentionally narrow. CLMPilot is a multi-tenant
platform: each customer organisation is an isolated tenant, so the
catalogue below describes the roles \emph{within} a single tenant,
mapping onto the functions that already exist in a Mittelstand
legal-operations team. Above them sits a platform-level system
administrator who provisions and isolates the tenants themselves; that
role belongs to the operator rather than the customer, and is treated
with the architecture in Chapter~\ref{chap:arch}.

\subsection{Actor catalogue}
\label{sec:actors-catalogue}

Table~\ref{tab:actors} catalogues the four per-tenant actors and the
use cases each one initiates.

\begin{table}[H]
  \centering
  \caption{Actors recognised by CLMPilot, with their primary use cases.}
  \footnotesize
  \begin{tabularx}{\linewidth}{|>{\bfseries}l|X|}
    \hline
    Actor & Primary use cases \\
    \hline\hline
    Admin & Provision and decommission users; assign roles; configure
      organisation-wide policies; review the system audit log. \\
    \hline
    Legal~Manager & Create and edit contract records; attach document
      links; initiate approval workflows; consult the dashboard and
      reports; receive deadline reminders for owned contracts. \\
    \hline
    Approver & Receive approval requests; approve, reject or request
      changes; consult the history of past approval decisions in
      scope. \\
    \hline
    Auditor & Read-only access to the full event log; export the audit
      trail; reconstruct the state of any contract at any past date
      within the retention window. \\
    \hline
  \end{tabularx}
  \label{tab:actors}
\end{table}

The system clock is the fifth actor. It is the trigger for the scheduled use cases of the
deadline-management subsystem. We treat it separately to make the
provenance of system-initiated actions explicit in the audit log.

\subsection{Use-case diagram}
Figure~\ref{fig:usecase} gives a high-level view of the platform: each
use case inside the system boundary names one functional area, and every
actor is linked to the areas it drives. The four human roles specialise a
common \emph{User} and inherit the shared authentication case from it,
while the \emph{System clock} is a secondary actor that drives the
scheduled deadline and renewal reminders. The individual use
cases inside each area, with their relationships and constraints, are
expanded one subsystem per figure in
Figures~\ref{fig:uc-identity}--\ref{fig:uc-reporting}.

\begin{figure}[H]\centering
  \IfFileExists{Chapter2/figures/usecase-overview.pdf}
    {\includegraphics[width=0.92\linewidth,keepaspectratio]{usecase-overview}}
    {\fbox{\parbox{0.7\linewidth}{\centering\vspace{2em}
      \textit{Diagram to be rendered from} \texttt{figures/usecase-overview.puml}\vspace{2em}}}}
  \caption{High-level use-case diagram for CLMPilot: each use case names one
           functional area, the human roles specialise a common \emph{User},
           and the \emph{System clock} is the secondary actor that drives the
           scheduled reminders.}
  \label{fig:usecase}
\end{figure}

Each area is then expanded into its individual use cases. Identity and
access (Figure~\ref{fig:uc-identity}) requires every authentication to
include a second factor, mandatory for administrators, and lets the
Admin provision users, assign roles and configure organisation-wide
policies and custom fields.

\begin{figure}[H]\centering
  \includegraphics[width=\linewidth,keepaspectratio]{uc-identity}
  \caption{Detailed use cases -- identity and access. Every authentication
           \emph{includes} a second factor, mandatory for administrators.}
  \label{fig:uc-identity}
\end{figure}

Contracts and documents (Figure~\ref{fig:uc-contracts}) covers the
registry and its document links: archiving never hard-deletes a record,
and intelligence-assisted capture extends the attachment of a document
link.

\begin{figure}[H]\centering
  \includegraphics[width=\linewidth,keepaspectratio]{uc-contracts}
  \caption{Detailed use cases -- contracts and documents. Archiving never
           hard-deletes a record, and intelligence-assisted capture
           \emph{extends} the attachment of a document link.}
  \label{fig:uc-contracts}
\end{figure}

Approvals and deadlines (Figure~\ref{fig:uc-approvals}) combine the
workflow -- where initiating an approval includes selecting a template,
and a reject or request-changes outcome returns the contract to its
initiator -- with the time-driven reminders the system clock raises
before each notice window.

\begin{figure}[H]\centering
  \includegraphics[width=\linewidth,keepaspectratio]{uc-approvals-deadlines}
  \caption{Detailed use cases -- approvals and deadlines. Initiating an
           approval \emph{includes} selecting a template; a reject or
           request-changes outcome \emph{extends} the decision by returning
           the contract to its initiator.}
  \label{fig:uc-approvals}
\end{figure}

Dashboard, reporting and audit (Figure~\ref{fig:uc-reporting}) groups
the portfolio views and reports, where scheduled reporting extends
report generation, with the read-only audit capabilities through which
any past contract state can be reconstructed within the ten-year
retention window.

\begin{figure}[H]\centering
  \includegraphics[width=\linewidth,keepaspectratio]{uc-reporting-audit}
  \caption{Detailed use cases -- dashboard, reporting and audit. Scheduled
           reporting \emph{extends} report generation; any past contract
           state is reconstructible within the ten-year retention window.}
  \label{fig:uc-reporting}
\end{figure}

Across these areas, the enterprise capabilities of
Section~\ref{sec:fr-enterprise} -- intelligence-assisted capture,
electronic signature, post-signature obligation tracking, scheduled
reporting and per-tenant custom fields -- extend the core registry rather
than replacing it.

\subsection{Detailed use-case descriptions}
\label{sec:uc-descriptions}

Three of these use cases carry the most consequential behaviour of the
platform: the creation of a contract record, the approval workflow, and
the reconstruction of past contract state for an audit.
Tables~\ref{tab:uc-contract-creation}--\ref{tab:uc-audit-reconstruction}
describe each of them in the structure of a standard use-case
description -- goal, actors, preconditions, main scenario, alternative
and exception flows, and postconditions -- using the same actor names
and «include»/«extend» semantics as the diagrams above.
Table~\ref{tab:uc-contract-creation} opens with the entry point of every
other capability: the registration of a contract.

\begin{table}[H]
  \centering
  \caption{Use-case description: register a contract.}
  \footnotesize
  \begin{tabularx}{\linewidth}{|>{\bfseries}l|X|}
    \hline
    Goal            & Register a new contract as a first-class record in
                      the organisation's contract registry. \\ \hline
    Actor(s)        & Legal~Manager (primary). \\ \hline
    Preconditions   & The Legal~Manager is authenticated in their tenant
                      and holds a role authorised to create contract
                      records. \\ \hline
    Main scenario   & \begin{enumerate}[nosep,leftmargin=*]
                        \item The Legal~Manager opens the contract
                          registry and starts a new record.
                        \item The Legal~Manager supplies the counterparty,
                          the contract category, the start and end dates,
                          the notice window and the contract value.
                        \item The Legal~Manager attaches a document link
                          to the file that materialises the contract in
                          the organisation's own storage («extend»:
                          intelligence-assisted capture proposes
                          counterparty, dates, value and detected clauses
                          for confirmation).
                        \item The Legal~Manager confirms the record; the
                          platform validates it.
                        \item The platform creates the contract in
                          \textit{draft} status, records the creation in
                          the audit log, and derives the contract's
                          deadline reminders from its dates.
                      \end{enumerate} \\ \hline
    Alternative /   & \textit{3a.} No document is attached: the record is
                      created without a link, and one can be attached
                      later. \textit{3b.} The intelligence proposal is
                      absent or wrong: the Legal~Manager fills or corrects
                      the fields manually. \textit{4a.} Validation fails
                      (missing mandatory attribute, inconsistent dates):
                      the platform rejects the submission and returns the
                      Legal~Manager to the form. \\
    exception flows & \\ \hline
    Postconditions  & The contract exists in the registry in
                      \textit{draft} status; its creation is immutably
                      recorded in the audit log; its deadline reminders
                      are scheduled against its notice window and
                      expiry. \\ \hline
  \end{tabularx}
  \label{tab:uc-contract-creation}
\end{table}

Table~\ref{tab:uc-approval-workflow} then follows a contract through the
approval chain it must complete before it becomes active.

\begin{table}[H]
  \centering
  \caption{Use-case description: approve a contract.}
  \footnotesize
  \begin{tabularx}{\linewidth}{|>{\bfseries}l|X|}
    \hline
    Goal            & Take a contract through an ordered chain of recorded
                      approval decisions before it becomes active. \\ \hline
    Actor(s)        & Legal~Manager (initiator); Approver (decides on the
                      steps addressed to them). \\ \hline
    Preconditions   & The contract record exists and is not yet active; a
                      workflow template describing the ordered approval
                      steps is available; the Legal~Manager is authorised
                      to initiate approvals. \\ \hline
    Main scenario   & \begin{enumerate}[nosep,leftmargin=*]
                        \item The Legal~Manager initiates an approval
                          request on the contract («include»: selecting
                          the workflow template that defines the ordered
                          steps and their Approvers).
                        \item The platform opens the first step and
                          notifies its Approver(s).
                        \item An Approver reviews the request and records
                          the decision \textit{approve}, with an optional
                          comment.
                        \item The platform records the decision -- author,
                          timestamp and comment -- immutably, and advances
                          the workflow to the next step.
                        \item Steps 2--4 repeat until every step is
                          approved.
                        \item The workflow completes and the contract
                          becomes \textit{active}.
                      \end{enumerate} \\ \hline
    Alternative /   & \textit{3a.} The Approver records \textit{reject}:
                      the workflow is interrupted and the contract returns
                      to its initiator («extend»: return contract to
                      initiator), with the decision and its comment
                      recorded. \textit{3b.} The Approver records
                      \textit{request changes}: as 3a, with the requested
                      changes recorded for the initiator. \\
    exception flows & \\ \hline
    Postconditions  & Every decision is immutably recorded with its
                      author, timestamp and comment, and is not editable
                      after the fact; on full approval the contract is
                      active; on interruption the contract is back with
                      its initiator and the workflow's record is
                      preserved. \\ \hline
  \end{tabularx}
  \label{tab:uc-approval-workflow}
\end{table}

Finally, Table~\ref{tab:uc-audit-reconstruction} describes the audit
consultation that the compliance posture must support at any time.

\begin{table}[H]
  \centering
  \caption{Use-case description: reconstruct past contract state for an
           audit.}
  \footnotesize
  \begin{tabularx}{\linewidth}{|>{\bfseries}l|X|}
    \hline
    Goal            & Reconstruct the state of a contract as of any past
                      date, and export the audit trail in a format that
                      survives the application. \\ \hline
    Actor(s)        & Auditor (primary). \\ \hline
    Preconditions   & The Auditor is authenticated with read-only audit
                      access; the requested date lies within the ten-year
                      GoBD retention window. \\ \hline
    Main scenario   & \begin{enumerate}[nosep,leftmargin=*]
                        \item The Auditor selects a contract and a past
                          date of interest.
                        \item The platform reconstructs the contract's
                          state as of that date from its immutable
                          history, even where the current business state
                          has since changed.
                        \item The platform presents the reconstructed
                          state together with the actions recorded up to
                          that date -- one row per action, with the actor,
                          the timestamp, the contract identifier and the
                          action type.
                        \item The Auditor exports the audit trail as a
                          tabular file usable in spreadsheet software.
                      \end{enumerate} \\ \hline
    Alternative /   & \textit{1a.} The Auditor consults the full event log
                      across the portfolio instead of a single contract:
                      the same read-only view and export apply to every
                      recorded action in scope. \\
    exception flows & \\ \hline
    Postconditions  & The Auditor holds a reproducible view of the
                      contract as of the requested date and, where
                      exported, a tabular audit file independent of the
                      application's runtime; no record was altered by the
                      consultation. \\ \hline
  \end{tabularx}
  \label{tab:uc-audit-reconstruction}
\end{table}

%--------------------------------------------------------------------
\section{Functional requirements}
\label{sec:functional-requirements}

The functional requirements that follow flow directly from the four pain
points of the problem statement
(Section~\ref{sec:problem-statement}) and are exercised through the use
cases of the previous section. They are stated at the level of capability,
not implementation: each subsection identifies the actor that initiates
the use case, the outcome the platform must produce, and the constraints
the surrounding context imposes. The architectural decomposition into
domains, commands and events is deferred to Chapter~\ref{chap:arch}.

\subsection{Contract registry}
The contract registry is the platform's central object and the entry
point for every other capability. The Legal~Manager creates a contract
record by supplying the counterparty, the contract category, the start
and end dates, the notice window and the contract value. The same actor
edits these attributes over the contract's lifetime and decommissions
the record by archiving rather than deleting it -- the immutability
requirement of Section~\ref{sec:nfr-audit} forbids hard deletion. The
registry supports filtering by status, category, business unit and
counterparty, full-text search on free-form fields, and pagination over
the result set. Performance targets for these operations are stated in
Section~\ref{sec:nfr-perf}.

\subsection{Document linking}
A contract record is connected to the document that materialises it
through a \textit{link} -- a typed reference to a file held in the
customer's own document store (Google~Drive, SharePoint, or any
HTTP-addressable location). The Legal~Manager attaches and detaches
links; the platform resolves them at display time, surfacing the file
name, last-modified timestamp and a click-through to the original
location. The platform never copies the document contents into its own
storage: the integration-first stance of
Section~\ref{sec:competitive-landscape} is enforced at this boundary.

\subsection{Deadline and renewal management}
Every contract carries up to three temporal markers: its expiry date,
the start of its notice window, and -- where applicable -- its
auto-renewal date. The platform must surface each of these markers
before it passes, through a reminder workflow that emits notifications
at 90, 60 and 30~days before the notice window opens, and again on the
day the window closes. The notification target is the contract's
designated owner, by default the Legal~Manager who created the record.
The reminder schedule is generated by the platform itself: the actor
who triggers the use case is the system clock, not a human user.

\subsection{Approval workflows}
A contract under negotiation moves through a sequence of approvals
before it is signed. The Legal~Manager initiates an approval request
and selects a workflow template -- a sequence of approval steps, each
addressed to one or several Approvers. Each Approver receives a request
and answers with one of three decisions: approve, reject, or request
changes. A reject or request-changes outcome interrupts the workflow
and returns the contract to the initiator; an approval advances it to
the next step. The platform records every decision, its author, its
timestamp and any comment, and these records are not editable after
the fact.

\subsection{Compliance and audit trail}
Every action that changes the state of a contract -- creation, edit,
link attachment, approval decision, archiving -- is recorded in an
immutable audit log. The Auditor accesses the log read-only and exports
it in a format that survives the application: a tabular file usable in
spreadsheet software, with one row per recorded action, and columns for
the actor, the timestamp, the contract identifier and the action type.
The log must satisfy the GoBD retention requirement of ten years and
must be reproducible at any date within that window, even after the
underlying business state has changed.

\subsection{Dashboard and reporting}
The dashboard is the Legal~Manager's daily view. It surfaces the
portfolio at a glance: counts of active, expired and pending contracts;
the contracts whose notice windows open in the next 30, 60 and 90~days;
the approval requests awaiting the user's decision; and the aggregate
value of the active portfolio. Beyond the standing dashboard, the
platform supports parametric reports -- contracts by counterparty,
spend by quarter, renewals by business unit -- to answer the kind of
question the problem statement identifies as routinely unanswered.

\subsection{Identity and access management}
Identity and access management (IAM) is a precondition for every other
capability. The Admin creates user accounts, assigns roles and revokes
access; users authenticate with an email address, a password and a
second factor delivered as a one-time code by email. The platform enforces role-based access
control along the actor catalogue of
Section~\ref{sec:actors-catalogue}: the same query can return different
projections depending on the caller's role, and any command that
exceeds the caller's authority is refused before it reaches the domain.
The reset, invitation and lockout flows are required at this layer
because they are the everyday surface through which IAM is exercised
by non-Admin users.

\subsection{Enterprise capabilities}
\label{sec:fr-enterprise}
Beyond the core registry, the platform is required to support a tier of
enterprise capabilities that deepen the response to the same four pain
points. Document linking is expected to trigger \textit{intelligence-assisted
capture}: when a contract's document is attached, the platform proposes
counterparty, dates, value and detected clauses for the Legal~Manager to
confirm, rather than leaving every field to manual entry. The platform must
take a contract through \textit{electronic signature} with an external
counterparty and reflect the signed outcome on the record, so that execution
is part of the tracked lifecycle rather than an out-of-band step.
\textit{Per-tenant custom fields} let each organisation extend the contract
schema to its own taxonomy without a code change. \textit{Post-signature
obligation tracking} expands standard deadline management, ensuring the
organization monitors and fulfills all operational deliverables throughout
the active lifecycle of the contract. And \textit{advanced, schedulable
reporting} answers the portfolio questions of the problem statement on a
recurring basis rather than on demand. These capabilities are stated here
as requirements; their realisation is consolidated in Chapter~\ref{chap:realisation}.

%--------------------------------------------------------------------
\section{Non-functional requirements}

Non-functional requirements describe the qualities the platform must
exhibit while delivering the functional capabilities of the previous
section. We group them along seven axes -- security, compliance,
availability, performance, auditability, internationalisation and
maintainability -- each justified by a constraint of the deployment
context rather than by a feature in isolation.

\subsection{Security}
\label{sec:nfr-security}
All traffic between the client and the platform is carried over
Transport-Layer Security (TLS) in its current widely-deployed version;
data at rest in the event store and the relational projections is
encrypted with a key under the operator's control. Every command and
query is authenticated; authorisation is enforced before the request
reaches the domain layer, on the basis of the caller's role. Passwords
are stored as salted hashes computed with a modern key-derivation
function. A second factor of authentication -- a one-time code delivered by
email -- is mandatory for Admin accounts and offered to every other
role.

\subsection{Compliance and data residency}
\label{sec:nfr-compliance}
The platform must satisfy three regulatory baselines simultaneously.
GoBD requires that contract records be retained immutably and
recoverable in their state as of any past date, for a period of up to
ten years. The GDPR requires that
personal data be processed under documented purposes, subject to access
and erasure requests, and stored within the European Union. ISO~27001
imposes a documented information-security management system, with
access controls, change tracking and incident response procedures. The
production environment is hosted in the European Union by default,
under a contract with an EU-resident provider; no customer data is
processed or stored outside the EU without an explicit opt-in.

\subsection{Availability and reliability}
The minimum viable product (MVP) carries an availability target rather
than a contractually committed Service-Level Objective (SLO):
99.5\% monthly during DACH business hours and
99.0\% outside them, to be validated against operational
data once production traffic is observed. Recovery-Point Objective
(RPO) is set at 24 hours, sustained by nightly off-site
backups; Recovery-Time Objective (RTO) at 4 hours. These figures
are MVP targets and will be tightened or relaxed based on customer
commitments in later phases. The platform degrades gracefully when
external document stores are unreachable: contract metadata and
workflow remain available, and document previews surface a
transient-error state rather than failing the surrounding page.

\subsection{Performance}
\label{sec:nfr-perf}
The performance budget for the MVP is expressed as engineering
targets to be measured and tuned against, not as contractual
guarantees. On a contemporary EU broadband connection, median
page-load latency aims for under one second on the dashboard and the
contract list; search and filter operations over a portfolio of ten
thousand contracts aim for under five hundred milliseconds; and
document-link resolution -- the round-trip to the external store,
plus the metadata fetch -- aims for under two seconds in the median
case. Failure modes degrade to the graceful state described above
rather than blocking the user interface.

\subsection{Auditability}
\label{sec:nfr-audit}
Every state-changing action in the platform is recorded as an immutable,
time-stamped, actor-attributed event. There is no primitive for
in-place update or destructive delete at the storage layer: a correction
is itself an event, layered on top of the prior history. The audit log
is exportable in a tabular format independent of the application's
runtime and is reconstructible at any point in time within the GoBD
retention window. This requirement aligns directly with the
compliance-by-architecture and shapes the architectural choices
presented in Chapter~\ref{chap:arch}.

\subsection{Internationalisation}
\label{sec:nfr-i18n}
English is the primary language of the user interface, with German
maintained alongside it; Other languages could be targeted for a
future expansion, with translation slots reserved in the user-interface
layer from the outset. Dates, numbers and currencies are formatted
according to the user's declared locale rather than the server's,
and the platform stores time-stamps in Coordinated Universal Time (UTC)
and renders them in the user's time zone. Email notifications inherit
the recipient's locale.

\subsection{Maintainability}
The platform was built by a single developer during a brief internship,
every architectural choice had to minimize the long-term maintenance burden.
The platform deliberately relies on a narrow, well-understood technology
stack rather than chasing a fragmented mix of "best tools".
To keep the project manageable for future developers, it incorporates
conventional commit messages, a predictable release-train cadence
(Section~\ref{sec:methodology}), and three distinct layers of automated
testing (unit, integration, and end-to-end). Finally, documentation is stored
directly alongside the code and compiled straight into the running application's
help system, preventing the docs from drifting out of date.

%--------------------------------------------------------------------
\section*{Conclusion}
This chapter has specified what the platform must do and how well it
must do it. We catalogued the actors -- four per-tenant human roles
specialising a common user, together with the system clock that drives
scheduled actions -- and expanded the use cases they initiate from a
single high-level overview down to the individual cases of each
functional area, describing the contract-creation, approval and
audit-reconstruction scenarios step by step. From those use cases we
derived the functional requirements, from the core contract registry to
the tier of enterprise capabilities, and the seven axes of
non-functional requirements that constrain how they must be delivered.

With the requirements specified, Chapter~\ref{chap:arch} presents the
structural response: the architecture through which these requirements
are satisfied by construction.

%==============================================================================
\end{spacing}
