\setcounter{chapter}{4}
\chapter{Improvements in the event-sourced system}\label{chap:future}
\graphicspath{{Chapter5/figures/}}

%==============================================================================
%% CHAPTER SHELL. Forward-looking engineering work on the comby framework and
%% the event-sourced core -- planned and designed in the remaining internship
%% time, not necessarily fully implemented. No sections yet (by request); the
%% three planned areas are recorded below as intent so the section breakdown
%% can be added later. Each builds on a seam named in Chapter III,
%% Section~\ref{sec:seams}, so this chapter references back rather than
%% re-introducing the architecture. Keep the engineer/architecture-level
%% register; UML (component / sequence / class / deployment) to justify each
%% design choice when the prose is written.
%%
%% Planned areas (future sections):
%%   (a) Insight extraction from broker communications.
%%       Extends the stateless extraction pipeline (Ch III, component view;
%%       Ch IV, intelligence section) from contract DOCUMENTS to the
%%       unstructured broker COMMUNICATIONS around a contract. Design the
%%       extraction interface, event-source the extracted insight, and feed it
%%       into the Contract / Deadline / Obligation contexts. NOTE: no code today
%%       -- this is design/R&D, to be flagged as such.
%%   (b) Multi-tenancy in the event-sourced framework.
%%       Deepen tenancy below the tenant (workspace scope already in comby but
%%       unused by CLMPilot domains), strengthen per-tenant isolation in the
%%       event store, and model cross-tenant consolidation for corporate-group
%%       customers -- without weakening the isolation the compliance posture
%%       depends on.
%%   (c) Scalability of comby and the event store.
%%       Snapshotting and projection persistence (no full-replay cold start),
%%       event-store archival/partitioning as the log grows unbounded, and the
%%       multi-instance broker-fanned topology already wired in
%%       Section~\ref{sec:deployment-topology} but not yet run. Single-writer
%%       assumptions and read-model rebuild are the design questions to resolve.
%%
%% A closing area when prose is written: a validation strategy (how each design
%% would be prototyped/benchmarked and what outcome would count as success).
%==============================================================================
\pagestyle{fancy}
\fancyhf{}
\fancyhead[R]{\bfseries\rightmark}
\fancyfoot[R]{\thepage}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\footrulewidth}{0pt}
\renewcommand{\chaptermark}[1]{\markboth{{\chaptername~\thechapter.\ #1}}{}}
\renewcommand{\sectionmark}[1]{\markright{\thechapter.\thesection~ #1}}

\begin{spacing}{1.2}
%==============================================================================

\section*{Introduction}
% intent: Position this chapter as forward-looking engineering R&D on the
% event-sourced system itself, building directly on the seams of
% Section~\ref{sec:seams}. State plainly that the work is planned and designed
% in the remaining internship time and is not necessarily fully implemented.
% Name the three areas -- broker-communication insight, multi-tenancy and
% scalability -- and the design-first method by which they are approached.
With the pilot realised, the remaining
engineering effort turns from the product to the event-sourced system it runs
on. This chapter sets out that forward-looking work -- improvements to the
\texttt{comby} framework and the event-sourced core: extracting insight from broker
communications, deepening multi-tenancy, and scaling the framework and the
event store. The work is planned and designed in the remaining internship
time, and the section breakdown is left to be
written. \emph{(Chapter shell; planned content recorded in the source for
later development.)}

%--------------------------------------------------------------------
\section*{Conclusion}
% intent: To be written once the design sections exist. Recap the planned
% improvements and how they complete the trajectory from a realised pilot to a
% stronger event-sourced foundation; lead into the report's general conclusion.
\emph{(To be written once the design sections are developed.)}

%==============================================================================
\end{spacing}
